package com.dtoind.lastvisit.model;

/**
 * The Category interface represents a category associated with a link or a visit.
 * Classes implementing this interface should provide methods to retrieve and set the category name.
 * This interface extends the BaseModel interface, which provides basic functionality for model objects.
 */
public interface Category extends BaseModel {}
